# Configuration Nginx pour mesh.jscheunersarl.ch
# À placer dans /etc/nginx/sites-available/mesh.jscheunersarl.ch

server {
    listen 80;
    server_name mesh.jscheunersarl.ch;

    # Redirection HTTP → HTTPS (si tu as un certificat SSL)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }
}

# Version HTTPS (recommandé avec Let's Encrypt)
# server {
#     listen 443 ssl;
#     server_name mesh.jscheunersarl.ch;
#
#     ssl_certificate /etc/letsencrypt/live/mesh.jscheunersarl.ch/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/mesh.jscheunersarl.ch/privkey.pem;
#
#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
